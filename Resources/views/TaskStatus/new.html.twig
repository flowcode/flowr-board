{% extends "FlowerCoreBundle::layout.html.twig" %}

{% block title %}{{ '%entity% creation'|trans({'%entity%': 'TaskStatus'}, 'TaskStatus') }} - {{ parent() }}{% endblock %}

{% block body %}
    <section class="content-header">
        <h1>{{ 'TaskStatus' | trans({}, 'TaskStatus')}}</h1>
        <ol class="breadcrumb">
            <li><a href={{ path('dashboard') }}><i class="fa fa-dashboard"></i> {{"Dashboard" | trans({}, 'TaskStatus')}}</a></li>
            <li><a href="{{ path('taskstatus') }}"> {{ '%entity% List'|trans({'%entity%': 'TaskStatus'}, 'TaskStatus') }}</a></li>
            <li class="active" >{{ 'New %entity%'|trans({'%entity%': 'TaskStatus'}, 'TaskStatus') }}</li>
        </ol>
    </section>        
    <section class="content">
        <div class="box">
            <div class="box-header">
                <h3 class="box-title">{{ '%entity% creation'|trans({'%entity%': 'TaskStatus'}, 'TaskStatus') }}</h3>
            </div>
            <div class="box-body">
                {{ form_start(form, {action: path('taskstatus_create')}) }}
                    {{ form_widget(form) }}
                    <p>
                        <button class="btn btn-primary" type="submit">{{ 'Create'|trans({}, 'TaskStatus') }}</button>
                    </p>
                {{ form_end(form) }}
            </div>
            <div class="box-footer">
                        
        <div class="btn-group record_actions">


        </div>

            </div>
        </div>
    </section>
{% endblock %}
